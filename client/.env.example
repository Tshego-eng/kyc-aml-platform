# Base URL of the KYC/AML backend API (see Server/.env.example for PORT).
# Do not commit a real .env file — copy this to .env and adjust per environment.
VITE_API_BASE_URL="http://localhost:5000/api"
